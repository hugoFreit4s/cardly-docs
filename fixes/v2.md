FIXES AND NEW FEATURES:

- Insert a link on the toast that shows up after cloning a deck from the community that leads the user to it's copy of the deck;
- Insert an icon to change themes, look at the project C:\projects\sisges\sisges-react-app and use the same icon that it uses;
- Insert an icon to lead to the user profile on top, on the side of the theme toggle icon and remove the "meu perfil" button on homepage;
- Homepage must have only the dashboards as the main subject and a sidebar, that is openned by an hamburger menu on the top-left; the hamburger must transition into a X that closes the menu, the menu should also close when the user slides from right to left;
- Add the classic door icon on the screen, this will be our EXIT button;
- Commit what is not commited 'till now and push it to the right repository, look the url's at rules.md and manipulate the commits as asked on todo.md;