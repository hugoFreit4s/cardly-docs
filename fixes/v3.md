FIXES AND NEW FEATURES:

- The Administrador should not see himself at the management panel;
- The disciplinas, inside the adm panel, should be kept inside each users tab, it means that what the Administrador will see in the tab Disciplinas is the name of each user, and when he clicks in any of them, shows the disciplinas of that specific user, the same applies for cards;
- Check if the ID being use to search on the management panel and for send friend request is the same used as a primary key on the DB, if it is we need to change that with the instructions below, if it isn't leave it alone;

INSTRUCTIONS TO REFACTOR ID LOGIC:

The primary key must not be used in the app to search or sent friend request, so each user needs to have an unique ID that is shown on the app, this ID should always be represented like this example: #124123 and should be generated by our backend on the register. Implement ID's for users that are already created too. The ID must be a data with easy access from the user, so display it below the username of the user on the profile page in a lighter tone, bold and italic, with a icon to copy it easily.